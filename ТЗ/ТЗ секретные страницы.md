model SecretPage {
  id        String   @id @default(cuid())
  slug      String   @unique
  title     String
  filePath  String   // например "secret-pages/pa-characters/index.html"
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([slug])
  @@index([isActive])
}

model UserAccess {
  id           String   @id @default(cuid())
  userId       String
  resourceType String   // "page", "challenge", "course", "feature"
  resourceId   String   // ID страницы, испытания, курса
  grantedBy    String   // "key_<keyId>" или "admin_<adminId>"
  grantedAt    DateTime @default(now())
  expiresAt    DateTime? // опционально
  createdAt    DateTime @default(now())

  @@unique([userId, resourceType, resourceId])
  @@index([userId])
  @@index([resourceType, resourceId])
  @@index([grantedBy])
}

model Key {
  id          String   @id @default(cuid())
  code        String   @unique
  actionsJson Json     // логика что даёт ключ
  maxUses     Int      @default(1)
  usedCount   Int      @default(0)
  expiresAt   DateTime?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([code])
  @@index([isActive])
}
model KeyUse {
  id        String   @id @default(cuid())
  keyId     String
  userId    String
  usedAt    DateTime @default(now())
  key       Key      @relation(fields: [keyId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([keyId, userId]) // один пользователь — один раз использовал ключ
  @@index([keyId])
  @@index([userId])
}


2. Админка секретных страниц
Расположение: /admin/secret-pages/ - добавить пункт меню после пункта "Курсы". Доступ админам и менеджерам. isAdmin isManager

Функции:

Список всех страниц (таблица: slug, title, description, isActive, кнопки)
Создание страницы
Редактирование
Удаление

3. Создание страницы
Форма:

Поле slug (латиница, только буквы/цифры/дефис)

Поле title

Поле description (опционально)

Поле для загрузки HTML файла (один файл)

Поле для загрузки изображений (множественный выбор)

Логика сохранения:

Проверить уникальность slug

Создать папку: public/secret-pages/[slug]/

Сохранить загруженный HTML как index.html в этой папке

Сохранить все изображения в папку public/secret-pages/[slug]/images/ с оригинальными именами

В HTML заменить все абсолютные ссылки на изображения на относительные (/secret-pages/[slug]/images/...)

Найти все src="http..."
Скачать каждое изображение в папку /secret-pages/[slug]/images/...
Заменить src на локальный путь

Записать в БД: slug, title, description, filePath = secret-pages/[slug]/index.html, isActive = true


4. Редактирование страницы
Форма:

Те же поля, что при создании

Slug нельзя менять (или можно с проверкой уникальности и переименованием папки на диске)

Можно загрузить новый HTML файл (заменяет старый)

Можно загрузить новые изображения (добавляются в папку images)

Список уже загруженных изображений с возможностью удаления

Логика:

При загрузке нового HTML — перезаписать index.html

При добавлении изображений — сохранить в папку images

При удалении изображения — удалить файл с диска

5. Удаление страницы
Логика:

Удалить запись из БД

Удалить папку public/secret-pages/[slug]/ рекурсивно (все файлы)

public/
  secret-pages/
    pa-characters/
      index.html
      images/
        batman.jpg
        shrek.jpg
    another-page/
      index.html
      images/
        ...

# Раздел админки - Доступы
Расположение
/admin/access/
Доступ к разделу и пункту меню - у менеджеров и админов

Пункт меню расположить под пукнтом "Курсы".

# Оформление
Верстка раздела в стиле других разделов, чтобы было ощущение что это единая система
/admin/psychologists
/admin/clients
/admin/courses
Заголовки, шрифты, цвета и т.д.


Функции
1. Список всех выдач

Таблица с колонками:

Пользователь (email, fullName) (в таблице User email зашифрован, учитывать это! При поиске использовать только поиск по полному email через хэш)

Тип ресурса (пока что - это только страница PAGE, оставить поле текстовым) Это именно секретные страницы. То есть secretPage а не просто pages.

Ресурс (название страницы)

Кем выдано (key_XXX, admin_YYY)

Дата выдачи

Срок действия (если есть)

Действия (кнопка "Отозвать")

Фильтры:

По пользователю (поиск по email) (только по полному  email через хэш. Поле email зашифровано. Смотри модель БД)
По конкретному ресурсу (выпадающий список)

Пагинация

2. Выдача доступа вручную

Кнопка "Выдать доступ"
Модальное окно с формой:
Поиск/выбор пользователя (по email) (опять же, по хэш мейла)
Тип ресурса (пока прям пишем вместо поля ввода слово "Секретная страница")
Выбор конкретного ресурса (выпадающий список)

Поле "Срок действия" (опционально, дата)
Кнопка "Выдать"

При сохранении:
Проверить, нет ли уже такой записи (userId + resourceType + resourceId)
Если нет — создать, grantedBy = "admin_{adminId}"

3. Отзыв доступа

Кнопка "Отозвать" в таблице
Подтверждение (модалка: "Удалить доступ пользователю X к ресурсу Y?")
Удалить запись из UserAccess

4. Фильтрация по источнику

Быстрый фильтр: "Выдано ключами", "Выдано админами"

Связь с другими разделами
При удалении страницы (из админки страниц) — автоматически удалить все записи UserAccess с resourceType="page" и resourceId=удалённой страницы



## ТЗ: API route для отдачи секретных страниц
Логика: пользователь вводит секретный урл.
Если всё хорошо и доступ у него есть - он открывается.
По сути, прокси-прослойка к реальному файлу, который мы загружали в сикрет пейджес.

### Расположение
`/app/secret-page/[id]/route.ts`

### Логика работы

**1. Получить параметр id из URL**
- Пример: `/secret-page/clx1234567890`
- id — это CUID из таблицы SecretPage

**2. Получить текущего пользователя**
- Взять сессию из куки через `getCurrentUser()`
- Если пользователь не авторизован — вернуть 401

**3. Найти страницу в БД**
- `prisma.secretPage.findUnique({ where: { id, isActive: true } })`
- Если не найдена — вернуть 404

**4. Проверить доступ**
- `prisma.userAccess.findFirst({ where: { userId: user.id, resourceType: "page", resourceId: id } })`
- Если записи нет — вернуть 403

**5. Прочитать файл с диска**
- Путь: `public/{filePath}` (filePath из БД)
- Через `fs.readFileSync` или `fs.promises.readFile`

**6. Вернуть HTML**
- `new Response(htmlContent, { headers: { "Content-Type": "text/html" } })`

### Обработка ошибок
- 401 — нет сессии
- 403 — нет доступа
- 404 — страница не найдена или не активна
- 500 — ошибка чтения файла

### Кэширование
- Заголовки: `Cache-Control: no-cache` (чтобы доступ отзывался мгновенно)

### Логирование (опционально)
- Записывать факт обращения (userId, pageId, timestamp) — можно потом, не в первой версии