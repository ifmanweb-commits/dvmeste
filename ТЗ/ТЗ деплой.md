Сейчас на сервере работает всё так

---

## Текущая схема работы

| Компонент | Где запущен | Как |
|-----------|-------------|-----|
| **Caddy** | На **хост-системе** (VDS) | Установлен через `apt`, слушает порты 80/443, проксирует запросы на `localhost:3000` |
| **WebPush сервис** | В **Docker** | Контейнер `webpush-app`, слушает порт 3000 внутри контейнера, проброшен на `localhost:3000` на хосте |
| **База данных SQLite** | В **Docker** (файл на хосте) | Монтируется volume `./data:/app/data` — файл БД лежит на хосте, но используется контейнером |
| **Загруженные картинки** | В **Docker** (папка на хосте) | Монтируется volume `./uploads:/app/uploads` |

---

## Как запрос доходит до приложения

```
Пользователь → push.psy-zrosta.ru → Caddy на хосте (порт 443) → прокси на localhost:3000 → Docker контейнер webpush-app → ответ
```

---

## Почему так сделано

- **Caddy на хосте** — проще управлять SSL, обновлять независимо от Docker
- **Приложение в Docker** — изолировано, легко пересобрать, не засоряет хост-систему
- **БД и uploads на хосте** — данные не теряются при пересборке контейнера

---

На этом же сервере мне нужно запустить текущий проект dvmeste.ru

База postgres должна работать в отдельном контейнере, в отдельном - app-dvmeste

app-dvmeste не должен конфликтовать с существующим сервисом push.psy-zrosta.ru
тот тоже работает на next и 3000 порту.

app-dvmeste должен публиковать порт на хосте как 127.0.0.1:3001:3000 (только localhost, чтобы Caddy на хосте мог проксировать).

Переменные нужно брать из .env
обязательно указать `DATABASE_URL` с хостом `postgres`

В `Dockerfile` установить `postgresql-client` и `gzip`

Добавить: в сервис postgres секцию healthcheck
Чтобы приложение не стартовало раньше, чем PostgreSQL поднялся.

Нужно сделать докер-компоуз, который будет поднимать базу и сайт.
Что ещё? проверить package и всё что нужно. Убедиться, что всё заработает через CI/CD

## Инициализация базы данных

1. При первом деплое (новая БД, нет данных) используем `npx prisma db push` — команда создаст схему БД на основе `schema.prisma`

2. Для последующих деплоев (когда БД уже существует) использовать `npx prisma migrate deploy` — применять только новые миграции

3. Добавить проверку в CI/CD: если БД пустая — `db push`, иначе — `migrate deploy`

Мне нужно разделить процесс установки и первого запуска сайта и процесс обновления кода на нём.
Запуск будет после того, как я сделаю git clone
Потом мне нужны инструкции что именно я должен выполнить, чтобы 
1) создалась и заработала база с правильными таблицами
2) заполнилась базовой информацией.
3) скачались все зависимости и установились для самого сайта и он скомпилировался и поднялся с правильными портами, чтобы нашёл базу и его можно было проксировать с кэдди на сервере.

Возможно, имеет смысл делать поэтапно. Сначала - поднять базу. Потом наполнить базу сид-скриптом npx tsx prisma/seed-initial.ts
Потом - поднимать сайт.